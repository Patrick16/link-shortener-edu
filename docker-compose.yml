services:
  # TODO: nginx (load balancer), auth-api, link-api, redirect-api,
  # shortener-service, traffic-service, rabbitmq, redis, mongo,
  # postgres-users, postgres-clicks — plus shards from docker-compose.shard*.yml
